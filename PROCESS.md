# CJ 체인 작업 컨벤션 (AGENTS.md 하네스 기반 업그레이드)

이 문서는 `C:\HTB\AGENTS.md`(HTB 단일-머신 write-up 하네스)의 구조와 원칙 중
이 CJ 다단계 체인 작업(43.200.51.235 → 3.37.135.243 → 13.125.104.131)에
그대로 적용 가능한 부분을 채택하고, 맞지 않는 부분은 명시적으로 대체한 버전이다.
기존 파일은 이동/재구성하지 않고, 이 문서 이후의 신규 산출물부터 적용한다.

2026-08-19 업데이트: `Downloads\AI_Solver\New project`(팀원의 별도 자동화 솔버
프로젝트)의 문서 컨벤션 중 §2, §4, §5, §7을 추가 반영. 해당 프로젝트의 코드/아키텍처는
우리 워크플로우와 무관하므로 제외했고, 순수 문서화 관례만 가져왔다.

## 0. AGENTS.md와의 차이점 (의도적 이탈)

| 항목 | AGENTS.md 원칙 | 이 체인에서의 실제 |
|---|---|---|
| 대상 범위 | 단일 `TARGET`, CIDR/피벗 확장 금지 | **다단계 피벗이 스코프 자체** — 43.200→3.37→13.125는 각각 별도 `<TARGET>/` 폴더로 관리하되 상위 체인 관계를 root README에 기록 |
| 실행 환경 | VPN 연결된 WSL 필수, Windows에서 직접 공격 금지 | HTB VPN 랩이 아닌 실공인 IP 대상 승인된 실습 — Windows/PowerShell/Bash 직접 실행 허용 (기존과 동일 유지) |
| 사실 태깅 | `[확인됨]`/`[외부 검증됨]`/`[추론]`/`[미확인]` | 기존 `[자체 확인]`/`[팀원 힌트]`/`[재현 필요]` + `[반증됨]`(§2 신규) 유지 — 팀원 정보 출처를 명시적으로 남기는 게 이 체인의 핵심이므로 AGENTS.md 4단계보다 우선 적용 |
| Write-up 구조 | 단일 파일, 16절 고정 | 대상별 `notes/pentest-report.md` 유지 + 최종 통합 시 AGENTS.md §10 구조 참고 |

## 1. 채택한 것 — 증적 ID 체계 (AGENTS.md §7)

앞으로 만드는 신규 증적 파일은 발생 순서대로 `E01`, `E02`... ID를 부여하고
각 대상 폴더의 `notes/evidence-index.md`에 기록한다. **기존 파일은 소급 리네임하지
않고, evidence-index.md에서 기존 파일명 → ID로 매핑만 한다.**

최소 필드: `ID`, `시각(KST)`, `단계`, `명령/요청`, `파일 경로`, `핵심 확인 내용`, `민감도`, `상태`

## 2. 채택한 것 — 반증됨(`[반증됨]`) 태그 추가

기존 3태그(`[자체 확인]`/`[팀원 힌트]`/`[재현 필요]`)에는 "직접 테스트해서 아니라고
확인된" 경우를 위한 태그가 없어 "실패 사례"로 어색하게 풀어써 왔다. 4번째 태그를 추가한다:

- `[자체 확인]` — 직접 실행/응답으로 확보
- `[팀원 힌트]` — 팀원 전달, 자체 미실행/미검증
- `[재현 필요]` — 팀원 힌트뿐이고 자체 재현 시도했으나 미완료(예: 연결 단절)
- **`[반증됨]`** — 정적분석/가설상으로는 성립해 보였으나 **실제 동적 검증으로 부정된 경우**
  (예: `43.200.51.235/notes/pentest-report.md` §19-2 `Upload.java` — 코드상 임의 경로 쓰기가
  가능해 보였으나 실제 업로드 테스트에서 Struts2 인터셉터가 basename만 사용함을 확인 →
  이 항목은 앞으로 `[자체 확인 — 실패]` 대신 `[반증됨]`으로 표기)

## 3. 채택한 것 — 성공 판정 기준 (AGENTS.md §6)

- `HTTP 200`/연결 성공만으로 익스플로잇 성공을 단정하지 않는다 (기존에도 지켜온 원칙 — RCE는
  파일쓰기가 아니라 타이밍 사이드채널로 최종 검증한 사례 참고)
- 코드 실행 후에는 `id`, `whoami`, 파일 소유권 등 직접 증적을 확인한다
- 컨테이너/root 판정은 `/.dockerenv`, `uid=0` 등 직접 증적으로만

## 4. 채택한 것 — 동일 실패 반복 방지

같은 실패 액션(예: 연결 안 되는 대상에 대한 curl 재확인)을 **2~3회 이상 반복하지 않는다.**
그 지점에서 다음 중 하나를 한다:
- 가정을 재점검한다 (IP/포트/스코프가 맞는지)
- 요청 방식 자체를 진단한다 (SSRF 경유 vs 직접, 프록시 여부 등)
- 다른 공격 표면으로 피벗한다

반복 확인이 꼭 필요하면(예: 서버 재개 여부 체크) 사용자에게 몇 회째인지 명시하고 계속할지 확인한다.

## 5. 채택한 것 — 가용성 장애와 취약점 실패의 분리

**일시적 서비스 중단/연결 단절은 취약점 실패로 합치지 않고 별도의 가용성 사건으로 분류한다.**
예: 3.37.135.243/13.125.104.131의 연결 끊김은 "RCE 실패"가 아니라 "가용성 이슈"로 기록하고,
관련 공격 단계는 `[재현 필요 — 연결 복구 대기]`로 남긴다. 취약점 자체의 성립 여부와 별개로 다룬다.

## 6. 채택한 것 — Write-up 전 증적 감사 (AGENTS.md §9)

각 대상의 최종 보고서를 "완료"로 표시하기 전에 다음 3단계 중 하나로 판정하고 문서 최상단에 남긴다:

- `READY` — 핵심 공격 체인에 직접 증적 있음, 치명적 불일치 없음
- `READY_WITH_WARNINGS` — 체인은 성립하나 일부 팀원 힌트가 미재현 상태로 남음
- `BLOCKED` — 초기접근/root 흐름 모순, 또는 핵심 단계가 전부 팀원 힌트뿐이고 자체 재현 전무

## 7. 채택한 것 — 재개 절차 (서비스 복구 후)

연결 단절 후 대상이 다시 열렸을 때는 순서대로 진행한다:

1. 서비스 복구 확인 (`/health` 또는 최소 1회 상태 확인 요청)
2. 승인된 범위에서 상태 확인 1회만 추가 수행
3. 기존에 확인했던 라우팅 조건/응답이 그대로인지 재확인
4. 사전에 준비해둔 비파괴적 증명(예: `sleep N` 타이밍 체크)만 1회 실행
5. 그 이상의 실행형 명령(권한상승 시도 등)은 사용자 승인을 다시 받고 진행

지금 13.125.104.131 재연결 대기 중인 상황에 바로 적용 — 재연결 확인되면 이 절차대로
E17(`find` 권한상승) 재개.

## 8. 채택하지 않은 것

- WSL/VPN 강제, 단일 CIDR 제한 — 이 체인의 실제 작업 방식과 맞지 않아 미채택
- Obsidian 전용 callout 문법 (`> [!evidence]`) — 강제하지 않음, 필요시 자유롭게 사용 가능
- MITRE ATT&CK 매핑 — 요청 시에만
- AI_Solver 프로젝트의 완료 판정(플래그 획득 필수), PRIMARY/SPECIALIST 역할 분리, 노트 파일
  SHA-256 의무화 — 그 프로젝트의 자동화·팀 대조용 장치라 우리 워크플로우엔 과함

## 9. 대상별 폴더 (기존 구조 유지, 변경 없음)

```
C:\HTB\43.200.51.235\   notes/, http/, loot/, scripts/, scans/, logs/, artifacts/, attachments/, evidence_selftest/
C:\HTB\3.37.135.243\    notes/, http/, loot/, scripts/, scans/, artifacts/, attachments/
C:\HTB\13.125.104.131\  notes/, http/, loot/, scripts/, tools/  (scans/, attachments/ 미생성 — 필요시 추가)
```
