# Hack The Box 풀이 프로젝트 지침

이 저장공간은 사용자가 명시적으로 지정한 Hack The Box 랩, 머신, 챌린지 등 허가된 실습 환경만을 대상으로 한다. 모든 작업은 사용자가 제공한 단일 대상과 해당 머신의 공격 경계 안에서 수행한다.

## 1. 적용 우선순위와 기본값

- 사용자의 현재 요청과 명시적 설정이 이 문서의 기본값보다 우선한다.
- 기본 설정은 다음과 같다.

```yaml
platform: Hack The Box
language: Korean
writeup_mode: PRIVATE_STUDY
force_writeup: false
external_verification: OFFICIAL_ONLY
include_mitre_attack: false
include_cvss: OFFICIAL_ONLY
output_format: OBSIDIAN_MARKDOWN
```

- 프로젝트 설정 파일과 공통 가이드는 루트 또는 `Guides/`에 둘 수 있다. 특정 머신을 풀면서 새로 만든 모든 영구 파일은 반드시 해당 머신 폴더 안에 둔다.
- 기존 파일과 디렉터리는 사용자의 자료다. 별도 요청 없이 이동, 이름 변경, 삭제 또는 일괄 재구성하지 않는다.
- 내부 사고 과정은 출력하거나 문서화하지 않는다. 독자가 재검증할 수 있는 관찰, 가설, 검증 명령/요청, 결과, 판정 및 다음 결정만 남긴다.

## 2. 작업 시작에 필요한 입력

풀이를 시작하기 전에 다음 값을 확보한다.

- `MACHINE_NAME`: HTB에 표시된 정확한 머신/챌린지 이름
- `TARGET`: 사용자가 이번 세션에 지정한 대상 IP 또는 호스트
- 필요한 경우 `DOMAIN`: 실제로 확인되거나 사용자가 제공한 도메인
- 공개 여부: 별도 지정이 없으면 `PRIVATE_STUDY`

머신명을 기존 문맥이나 제공 자료에서 신뢰성 있게 확인할 수 없으면, 파일을 만들기 전에 사용자에게 머신명을 한 번만 묻는다. 대상 IP를 추측하거나 과거 세션의 IP를 재사용하지 않는다.

## 3. 머신별 디렉터리 원칙

작업 루트는 항상 다음과 같다.

```text
<PROJECT_ROOT>/<MACHINE_NAME>/
```

새 머신은 필요에 따라 아래 구조를 만든다. 빈 디렉터리를 만들기 위한 자리표시자 파일은 만들지 않는다.

```text
<MACHINE_NAME>/
├── README.md
├── notes/
│   ├── evidence-index.md
│   └── evidence-by-category.md
├── logs/
│   └── action-log.md
├── scans/
├── http/
├── scripts/
├── artifacts/
├── loot/
├── attachments/
│   └── evidence/
│       └── HTB_<MACHINE_NAME>/
└── HTB_<MACHINE_NAME>_Writeup.md
```

- 스캔 결과는 `scans/`, 원본 터미널 기록은 `logs/`, HTTP 요청·응답은 `http/`, 작성한 도구는 `scripts/`, 다운로드하거나 추출한 파일은 `artifacts/`, 머신 전용 자격 증명·플래그 등 민감한 랩 결과는 `loot/`에 둔다.
- 스크린샷과 핵심 증적은 `attachments/evidence/HTB_<MACHINE_NAME>/`에 둔다.
- 파일명은 단계와 내용을 식별할 수 있게 작성한다. 예: `E01_nmap_full_tcp.txt`, `E02_http_headers.txt`, `E05_initial_access_id.png`.
- 이미 존재하는 머신 폴더의 구조와 파일명은 존중한다. 새 결과물부터 위 원칙을 적용하고, 기존 자료는 사용자 승인 없이 재배치하지 않는다.
- WSL의 홈, `/tmp`, 현재 셸 디렉터리 등 머신 폴더 밖에 영구 결과를 남기지 않는다. 도구가 임시 파일을 강제로 만들면 위치와 용도를 기록하고, 작업 종료 시 에이전트가 만든 정확한 파일만 정리하거나 머신 폴더로 옮긴다.
- 대상 시스템에 업로드하거나 생성한 파일도 경로, 목적, 소유자/권한 및 정리 상태를 `notes/artifact-ledger.md` 또는 Write-up의 정리 절에 기록한다.

## 4. WSL 및 VPN 실행 규칙

- 네트워크 조사, 익스플로잇 검증, 리스너, 포트 포워딩과 리눅스 도구 실행은 VPN이 연결된 WSL 환경에서 수행한다. Windows 호스트에서 대상에 직접 스캔하거나 공격 트래픽을 보내지 않는다.
- 먼저 설치된 WSL 배포판을 읽기 전용으로 확인한다. 사용자가 배포판을 지정하지 않았다면 VPN과 `tun0`가 확인되는 기존 배포판을 사용한다. 새 배포판, 패키지 또는 시스템 서비스를 임의로 설치하지 않는다.
- 실행 전에 WSL 안에서 다음을 확인한다.

```bash
ip -4 addr show tun0
ip route get "$TARGET"
```

- `tun0`에 IPv4 주소가 없거나 대상 경로가 VPN 인터페이스를 통하지 않으면 네트워크 작업을 중단하고 상태를 알린다. VPN 설정 파일, 인증 정보 또는 토큰을 열거나 복사하거나 문서화하지 않는다.
- 사용자가 제공한 `TARGET`이 유효한 단일 대상인지 확인한다. CIDR, 인접 호스트, 다른 vhost, 인터넷 주소 또는 로컬 LAN으로 범위를 넓히지 않는다. 범위 확대가 필요하면 사용자의 명시적 승인을 먼저 받는다.
- 매 새 세션과 머신 리셋 후 대상 IP를 다시 확인한다. 이전 인스턴스 IP를 고정값처럼 재사용하지 않는다.
- 공격자 주소는 `tun0`에서 구하고 가능한 한 변수로 사용한다.

```bash
export TARGET=<USER_SUPPLIED_TARGET_IP>
export DOMAIN=<CONFIRMED_MACHINE_DOMAIN>
export LHOST=$(ip -4 -o addr show tun0 | awk '{print $4}' | cut -d/ -f1)
```

- 도메인은 제공되었거나 응답/인증서/리다이렉트 등에서 확인된 경우에만 사용한다. `/etc/hosts` 변경이 필요하면 정확한 대상 항목만 추가하고 그 변경을 기록한다.
- PowerShell에서 WSL 명령을 호출할 때는 PowerShell이 Bash 변수나 특수문자를 먼저 확장하지 않도록 인용을 점검한다. 실제 네트워크 명령은 WSL의 Bash 문맥에서 실행한다.
- WSL에서 프로젝트 폴더의 절대 경로를 확인하여 모든 `-o`, `--output`, `tee`, 다운로드 및 스크립트 경로가 `<MACHINE_NAME>/` 아래를 가리키게 한다.
- 리스너는 가능하면 `$LHOST`에만 바인딩한다. 임시 포트와 백그라운드 PID는 기록하고 종료 시 에이전트가 시작한 프로세스만 종료한다.

## 5. 허가 범위와 안전 기준

- 이 프로젝트의 공격 행위는 현재 사용자가 지정한 HTB 대상에만 허용된다.
- 인터넷, 회사/학교망, 로컬 LAN, 사용자 장치 또는 제3자 자산을 스캔하거나 공격하지 않는다.
- 무차별 대입, 대량 요청, 서비스 중단 가능성이 있는 기법은 문제 풀이에 꼭 필요하고 HTB 범위 내임이 분명할 때만 최소 속도와 최소 범위로 수행한다.
- 데이터 파괴, 서비스 중단, 대량 삭제, 지속성 설치는 풀이에 필요하지 않으면 수행하지 않는다. 검증은 영향이 가장 작은 방법을 우선한다.
- 대상에 만든 계정, 토큰, 저장소, 파일, 예약 작업, 키 또는 포워딩은 정확히 기록한다. 정리는 에이전트가 만든 항목으로 한정하고, 완료 여부를 직접 확인하지 못했으면 `[권장 정리]`로 표시한다.
- 개인 SSH 개인키, HTB 계정 토큰, VPN 구성/자격 증명, 개인 API 키 및 실제 개인 계정 정보는 읽어서 문서화하거나 결과물에 복사하지 않는다.

## 6. 재현 가능한 풀이 절차

각 단계는 다음 순환 구조를 따른다.

1. **목표**: 이번 단계에서 확인할 대상과 성공 기준을 적는다.
2. **관찰 및 가설**: 직전 증적에서 관찰한 사실과 검증할 가설을 구분한다.
3. **검증**: 실제 명령, HTTP 요청 또는 코드를 최소 범위로 실행한다.
4. **핵심 결과**: 원본 결과를 파일로 보존하고 판단에 사용한 줄을 추출한다.
5. **성공 판정**: 상태 코드나 연결 자체가 아니라 실제 영향을 검증한다.
6. **해석과 다음 결정**: 결과가 다음 행동으로 이어진 이유를 기록한다.
7. **증적**: 관련 파일과 증적 ID를 연결한다.

가능하면 각 명령의 원본 출력을 `tee` 또는 도구의 출력 옵션으로 머신 폴더에 저장한다. 명령과 출력을 구분하며, 사후에 성공한 것처럼 출력이나 명령을 재구성하지 않는다.

### 성공 판정

- `HTTP 200`만으로 익스플로잇 성공을 단정하지 않는다.
- `HTTP 500`이나 연결 종료만으로 실패를 단정하지 않는다. 파일 생성, 콜백, 명령 부작용 등 실제 결과를 확인한다.
- 코드 실행 뒤에는 상황에 맞게 `id`, `whoami`, `hostname`, 프로세스 실행 사용자, 파일 소유권/권한/내용을 확인한다.
- 리버스 셸 연결만으로 root를 단정하지 않는다. `uid=0(root)`, root 소유 결과, root 전용 자원 접근 등 직접 증적을 확보한다.
- 컨테이너 여부는 `/.dockerenv`, cgroup, mount 등 실제 증적으로 확인한다.
- 컨테이너 내부 자격 증명으로 호스트 SSH에 로그인했다면 직접 탈출이 아니라 자격 증명 재사용일 수 있음을 구분한다.

### 6-1. 동일 실패 반복 방지

같은 실패 행동(예: 연결 안 되는 대상에 대한 재확인)을 **2~3회 이상 반복하지 않는다.**
그 지점에서 다음 중 하나를 한다:

- 가정을 재점검한다 (IP/포트/스코프가 맞는지)
- 요청 방식 자체를 진단한다 (경유 경로, 프록시 여부, 인증 상태 등)
- 다른 공격 표면으로 피벗한다

반복 확인이 꼭 필요하면(예: 서비스 복구 여부 체크) 사용자에게 몇 회째인지 명시하고 계속할지
확인한다.

### 사실과 추론

주요 주장은 작업 노트에서 다음으로 분류한다.

- `[확인됨]`: 로그, 응답, 파일, 소스, 스크립트 또는 읽을 수 있는 스크린샷에서 직접 확인
- `[외부 검증됨]`: 공식 공지, 공식 저장소, NVD/MITRE 또는 표준 문서에서 확인
- `[추론]`: 확인된 사실에 기반한 해석이나 직접 증적 없음
- `[미확인]`: 현재 자료로 단정 불가

최종 본문의 모든 문장에 태그를 붙이지는 않는다. 다만 핵심 공격 단계가 추론에 의존하거나 실행 사용자, 공격 조건, 내부 구조가 직접 검증되지 않은 경우에는 상태를 명시한다.

**결정적 값 보고 게이트**: 플래그, 자격 증명, 키, 토큰 등 결정적 값은 그 값이 실제로 나온
도구 실행 결과(파일 읽기, 명령 출력 등 이번 세션의 실제 tool result)가 존재할 때만
`[확인됨]`으로 인용한다. 그런 도구 결과 없이 값을 서술·재구성·추정해서 제시하지 않는다.
파일 읽기가 실패하거나 오류가 나면 값을 채워 넣지 말고 `[미확인 — 읽기 실패]`로 명시한다.
경량 모델로 세션을 진행 중일수록 이 게이트를 더 엄격히 적용한다 — "확보했다"는 서술 자체를
도구 결과 인용 없이는 하지 않는다.

## 7. 증적 관리

- 증적에는 발생 순서대로 `E01`, `E02`, `E03` 형식의 ID를 부여하고 `notes/evidence-index.md`에 관리한다.
- 최소 필드는 `ID`, `시각`, `단계`, `명령/요청`, `파일 경로`, `핵심 확인 내용`, `민감도`, `상태`, `카테고리`다.
- `카테고리`는 다음 통제 어휘 중 해당하는 값을 붙인다 (복수 태그 가능, 쉼표로 구분):
  `endpoint`(URL/API 경로) / `credential`(계정·비밀번호·키·토큰) / `host-ip`(대상·피벗 IP·호스트) /
  `file-path`(대상 시스템 파일 경로) / `process`(서비스·PID) / `vuln`(CVE/CWE) / `other`.
  예: `credential, host-ip` — 특정 호스트에서 확보한 자격 증명.
- 원본 로그와 HTTP 요청·응답은 수정하지 않는다. 정리본이 필요하면 별도 파일을 만들고 원본을 가리킨다.
- 다운로드한 실행 파일, 아카이브, 소스 또는 변형 가능성이 있는 증적은 가능하면 SHA-256을 기록한다.
- 스크린샷은 텍스트 증적의 보조 수단이다. 이미지 앞에 ID, 단계, 핵심 내용, 뒷받침하는 주장을 쓴다.
- 존재하지 않는 이미지 링크, 스크립트, 명령, 출력, 버전, 계정 또는 파일명을 만들지 않는다.
- 잘못된 경로를 발견해도 실제 이동/이름 변경이 확인되지 않았다면 문서에서 임의 수정하지 말고 경고로 남긴다.
- 서로 다른 자료가 충돌하면 원본 증적을 우선하되 자연스럽게 합치지 말고 `자료 간 불일치` callout으로 표시한다.

증적 우선순위는 다음과 같다.

1. 원본 터미널 로그, HTTP 요청·응답, 파일 내용, 소스 코드
2. 내용을 직접 판독할 수 있는 스크린샷
3. 사용자의 풀이 메모
4. 기존 Write-up 초안
5. 외부 분석 자료

### 7-1. 행동 타임라인 로그

증적 인덱스와 별개로, 대상에 실제로 명령·요청을 보낸 모든 행동을 발생 순서대로
`<MACHINE_NAME>/logs/action-log.md`에 기록한다. 코드 읽기, 분석, 사고 과정 등 대상에
아무것도 보내지 않은 행동은 기록하지 않는다.

최소 열: `Turn`, `시각(KST)`, `단계`, `행동(명령 요약)`, `대상`, `결과`, `증적ID`, `비고`.

- `Turn`은 해당 대상 폴더 안에서 1부터 순증하는 카운터로, 증적 ID(`E01`...)와는 별개다.
- `결과`는 성공 / 실패 / 차단 / 반증됨 / 가용성이슈 중 하나로 적는다.
- 그 행동이 `notes/evidence-index.md`에 증적을 남겼다면 해당 `증적ID`를 연결하고,
  아니면 빈칸으로 둔다.
- 이 로그는 "무슨 일이 있었는지"의 시간순 내러티브이고, evidence-index.md는 "무엇을
  확보했는지"의 증적 카탈로그다 — 둘을 합치지 않는다.

### 7-2. 결정적 증적 보고 전 자가 점검 게이트

플래그, root/관리자 자격 증명, private key 등 "결정적 증적"을 `evidence-index.md`,
`action-log.md`, Write-up 또는 사용자에게 보고하기 직전에 다음 3가지를 스스로 점검한다.

1. 이 값을 얻은 구체적 도구 호출(Read, Bash, HTTP 요청 등)이 이번 세션에 실제로 있었는가
2. 그 도구 호출의 tool result에 이 값이 정확한 문자열로 존재하는가
3. 위 두 가지 중 하나라도 아니오라면, 값을 인용하지 않고 `[미확인 — 직접 확인 필요]`로만
   남긴다 — 값을 지어내거나 그럴듯하게 재구성하지 않는다

이 게이트는 §2의 결정적 값 보고 게이트와 동일한 원칙을 증적 기록·행동 로그 작성 시점에
다시 적용하는 것이다. 특히 경량 모델로 세션을 진행 중일 때는 "확보했다/확인했다"는 말을
쓰기 전에 반드시 이 3단계를 거친다.

## 8. 취약점 분류와 외부 검증

공격 단계는 다음 중 가장 정확한 범주로 구분한다.

1. 공개 CVE
2. 자체 구현 결함과 관련 CWE
3. 위험한 서비스 구성
4. 자격 증명 재사용 또는 운영 보안 실패
5. 정상 기능의 위험한 노출 또는 오용
6. 미분류

- 버전 문자열만으로 CVE 영향을 확정하지 않는다. 대상 기능 활성화, 인증 조건, 네트워크 접근성 등 실제 공격 조건도 확인한다.
- 자체 제작 코드 결함에 임의의 CVE를 붙이지 않는다. 근본 원인이 명확한 경우 적절한 CWE를 사용한다.
- 외부 검증이 필요하면 현재 공식 자료를 웹에서 확인하고, 기술 주장은 제품 공식 보안 공지, 공식 GitHub Security Advisory, 공식 수정 PR/커밋, NVD/MITRE, 공식 릴리스 노트, RFC/표준 문서 순으로 우선한다.
- 기술 검증에는 공식/원문 자료를 우선하며, 개인 블로그, 공개 PoC와 업체 분석은 보조 자료로만 사용한다.
- 확인하지 않은 URL, 수정 버전, 영향 버전 또는 공격 조건을 만들지 않는다. 공식 근거를 찾지 못하면 `[외부 검증 필요 — 공식 공지를 확인하지 못함]`으로 표시한다.
- CVSS는 공식 공지나 NVD에 명시된 값만 사용한다. 임의 계산하거나 추정하지 않는다.
- MITRE ATT&CK는 사용자가 요청하거나 `include_mitre_attack: true`일 때만, 공식 기법과 명확히 대응되는 항목만 포함한다.

## 9. Write-up 전 증적 감사

최종 Write-up을 쓰기 전에 반드시 `# A. 작성 전 증적 감사`를 작성하고 다음 중 하나로 판정한다.

- `READY`: 머신명과 공격 체인이 일관되고 초기 접근부터 root 검증까지 핵심 직접 증적이 있으며 치명적 불일치가 없음
- `READY_WITH_WARNINGS`: 공격 체인은 성립하지만 일부 출력, 이미지, 명령 또는 외부 검증이 부족하여 경고가 필요함
- `BLOCKED`: 다른 머신 자료 혼합, 핵심 식별자 충돌, 초기 접근/root 흐름 모순, root 직접 증적 전무, 존재하지 않는 자료를 만들어야만 연결되는 공격 체인 등 치명적 문제가 있음

감사에는 다음을 포함한다.

1. 종합 판정과 짧은 근거
2. 번호가 붙은 차단 항목
3. 번호가 붙은 경고 항목
4. 자동 정규화 항목
5. 자료 목록
6. 공격 체인 완전성 표
7. 이미지/증적 경로 검사
8. 세션 종속 값 및 민감 정보 검사
9. 취약점 분류 검사
10. 본문에서 언급할 스크립트의 실제 존재 여부
11. 필요한 추가 증적을 마지막 한 번에 모은 목록

공격 체인 표는 최소한 열거, 초기 접근, 사용자 권한, 권한 상승, root 최종 검증을 다루고 각 주장에 필요한 증적, 확인된 증적과 상태를 적는다.

`BLOCKED`이고 `force_writeup: false`이면 감사까지만 작성하고 중단한다. `force_writeup: true`일 때만 Write-up을 계속하며 모든 문제 위치에 `[증적 부족]` 또는 `[모순 확인 필요]`를 표시한다.

### 9-1. 카테고리별 증적 뷰

Write-up 전 증적감사(§9)를 시작하기 직전, `notes/evidence-index.md`에서
`<MACHINE_NAME>/notes/evidence-by-category.md`를 파생 생성(재생성)한다. 세션 중
증적이 추가될 때마다 갱신하지 않는다 — 오직 증적감사 직전 1회.

- 파일 최상단에 `자동 생성 — evidence-index.md가 원본, 직접 편집 금지`를 명시한다.
- §7의 `카테고리` 통제 어휘별로 증적 ID를 그룹핑한 표를 만든다
  (예: `## endpoint`, `## credential`, `## host-ip` ... 각 절 아래 해당 증적 ID·핵심 확인 내용 요약).
- 태그가 여러 개인 증적은 해당하는 모든 카테고리 절에 중복 등장시킨다.
- evidence-index.md와 내용이 어긋나면 evidence-index.md를 우선하고 이 파일을 다시 생성한다.

## 10. Write-up 문서 규칙

Write-up은 한국어 Obsidian Markdown으로 작성하며 전체 문서를 하나의 코드 블록으로 감싸지 않는다. 파일 경로는 다음을 기본으로 한다.

```text
<MACHINE_NAME>/HTB_<MACHINE_NAME>_Writeup.md
```

문서는 다음 순서를 따른다.

1. `# A. 작성 전 증적 감사`
2. 종합 판정, 차단/경고/자동 정규화 항목
3. 자료 및 공격 체인 완전성 표
4. `# B. 최종 Write-up`
5. YAML frontmatter
6. `# HTB <MACHINE_NAME> Write-up`
7. 아래 본문 절과 필요한 부록

YAML 기본 형식:

```yaml
---
title: "HTB <MACHINE_NAME> Write-up"
machine: "<MACHINE_NAME>"
platform: "Hack The Box"
os: "<Linux|Windows|Unknown>"
difficulty: "<제공된 값|Unknown>"
date_started: "<제공된 날짜|Unknown>"
date_completed: "<제공된 날짜|Unknown>"
writeup_mode: "<PRIVATE_STUDY|PUBLIC_RELEASE>"
status: "<Completed|Partial>"
tags:
  - htb
  - cybersecurity
  - writeup
---
```

본문 구조:

- `0. 문서 범위 및 주의사항`
- `1. 개요`
- `2. 공격 흐름`: 실제 내용을 담은 Mermaid 흐름도와 한 줄 공격 체인
- `3. 실습 환경`: `$TARGET`, `$DOMAIN`, `$LHOST`, 실제 사용 도구
- `4. 공격 표면 요약`
- `5. 정보 수집`
- `6. 초기 접근`
- `7. 사용자 권한 획득`
- `8. 권한 상승 열거`
- `9. 권한 상승`
- `10. 권한 및 신뢰 경계 전환`
- `11. 취약점 요약`
- `12. 실패한 접근과 트러블슈팅`: 판단에 영향을 준 실패만
- `13. 실습 중 생성한 흔적과 정리`
- `14. 탐지 및 대응`: 근본 원인, 단기 완화, 근본 수정, 탐지 가능한 흔적
- `15. 핵심 학습 포인트`: 구체적이고 재사용 가능한 3~7개 항목
- `16. 참고 자료`: 실제 확인한 자료와 검증 용도
- `부록 A. 사용한 스크립트`: 실제 제공된 스크립트만
- `부록 B. 원본 스캔 및 응답`: 실제 원본이 있을 때만
- `부록 C. 증적 목록`

실제 머신에 해당하지 않거나 자료가 없는 하위 절과 부록은 억지로 채우지 않는다. 필요하면 `해당 없음` 또는 `[미확인]`으로 짧게 명시한다.

각 핵심 공격 단계는 다음 고정 소제목을 사용한다.

- 목표
- 관찰 및 가설
- 검증 명령 또는 요청
- 핵심 결과
- 성공 판정
- 취약점 원인과 공격 조건
- 해석과 다음 결정
- 증적

## 11. 명령, 출력, 이미지 문체

- 실제로 실행하거나 제공된 명령만 기록하며 재현에 필요한 순서를 바꾸지 않는다.
- Bash, PowerShell, Python, SQL, HTTP 등 코드 블록 언어를 정확히 지정한다.
- 명령과 서버 출력을 같은 코드 블록에 섞지 않는다.
- 대상과 공격자 IP는 가능한 한 `$TARGET`, `$LHOST`로 정규화한다. 실제 고정값이 꼭 필요하면 `풀이 당시 세션 종속 값`이라고 표시한다.
- 긴 출력은 본문에 핵심 줄만 남기고 원본 파일 또는 부록을 연결한다. 생략은 `...`로 표시하며 결과를 왜곡하지 않는다.
- 공격자가 실제로 통제한 입력, 그 입력이 전달된 지점, 누락된 검증, 발생한 영향을 구분한다.
- 취약점의 원인과 결과, `가능하다`와 `확인했다`, 초기 접근/횡적 이동/자격 증명 재사용/컨테이너 탈출/로컬 권한 상승을 정확히 구분한다.
- `매우 중요하다`, `주목할 만하다`, `흥미롭게도`, `놀랍게도`, `강력한 취약점이다`, `이로써 성공적으로`, `보안의 중요성을 알 수 있다` 같은 근거 없는 강조와 감상문을 피한다.
- 각 문단은 관찰, 해석 또는 다음 행동 중 하나 이상의 역할을 가져야 한다.

Obsidian 증적 형식:

```markdown
> [!evidence] E05 — 초기 명령 실행 검증
> `id` 출력에서 웹 서비스 계정 권한으로 명령이 실행됨을 확인했다.

![[attachments/evidence/HTB_<MACHINE_NAME>/E05_initial_access_id.png]]
```

이미지가 없거나 판독할 수 없으면 링크나 내용을 추측하지 않고 `[스크린샷 내용 확인 불가]`로 표시한다.

## 12. 민감 정보 및 공개 모드

### PRIVATE_STUDY

- 해당 HTB 머신에서만 유효한 계정/비밀번호와 user/root 플래그는 기록할 수 있다.
- 민감 값은 별도 callout 또는 `loot/`로 분리한다.
- 개인 SSH 개인키, HTB 계정 토큰, VPN 설정/자격 증명, 개인 API 키, 세션 쿠키는 기록하지 않는다.

### PUBLIC_RELEASE

- user flag: `[REDACTED — USER FLAG]`
- root flag: `[REDACTED — ROOT FLAG]`
- 비밀번호, 토큰, 쿠키, 임시 ID: `[REDACTED]`
- 대상 IP: `$TARGET` 또는 `<TARGET_IP>`
- 공격자 VPN IP: `$LHOST` 또는 `<LHOST>`
- 세션 종속 PID, WebSocket ID, 재설정 토큰과 임시 URL 식별자는 치환한다.
- 공개 가능 여부가 확인되지 않은 머신에는 문서 상단에 `[공개 전 HTB 정책 및 머신 상태 확인 필요]` 경고를 넣는다.

## 13. 완료 전 자체 검수

최종 전달 전 다음을 확인하되 장황한 체크리스트를 결과에 그대로 붙이지 않는다.

- 머신명이 폴더, 제목, 증적 경로 전체에서 일치한다.
- 공격 체인의 모든 전환에 증적이 있다.
- 초기 접근, 사용자 및 root 권한이 각각 직접 검증되었다.
- CVE, CWE, 위험한 구성, 자격 증명 재사용이 구분되었다.
- 세션 종속 값이 고정값처럼 서술되지 않았다.
- 존재하지 않는 이미지, 스크립트, 옵션, 출력 또는 참고 링크가 없다.
- 공개 모드의 플래그와 비밀 값이 제거되었다.
- 이미지마다 설명과 증적 ID가 있다.
- 공식 근거 없는 CVSS를 만들지 않았다.
- 실패 응답과 실제 성공 여부를 구분했다.
- 수정 방안이 확인된 근본 원인과 직접 연결된다.
- 대상 및 로컬에 만든 흔적과 정리 상태가 기록되었다.

발견한 문제는 증적 감사 또는 본문 경고에 반영한다. 불충분한 부분을 자연스럽게 지어내는 것보다 `[증적 부족]`, `[미확인]`, `[외부 검증 필요]`로 남기는 것을 우선한다.
